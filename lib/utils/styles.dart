import 'package:flutter/material.dart';

class PaletaCores{
  static const blueLogo = Color(0xFF000000);
  static const menuColor = Color(0xFFFFFFFF);
  static const bgColor = Color(0xFF828282);
  static const colorG1 = Color(0xFF3e2a78);
  static const colorG2 = Color(0xFF202348);

  static const defaultPadding = 20.0;
  static const defaultDuration = Duration(seconds: 4); // we use it on our animation
  static const maxWidth = 1080.0; // max width of our web

}